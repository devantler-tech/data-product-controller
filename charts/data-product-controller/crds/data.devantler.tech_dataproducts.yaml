---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: dataproducts.data.devantler.tech
spec:
  group: data.devantler.tech
  names:
    kind: DataProduct
    listKind: DataProductList
    plural: dataproducts
    singular: dataproduct
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .spec.version
      name: Version
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: DataProduct is the control-plane description of one independently
          operated data capability.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DataProductSpec defines a self-describing and composable
              data product.
            properties:
              description:
                description: Description states what data capability the product provides.
                minLength: 1
                type: string
              documentationUrl:
                description: DocumentationURL points to current product documentation.
                pattern: ^https://[^[:space:]]+$
                type: string
              id:
                description: ID is a stable URI for the product across clusters and
                  deployments.
                pattern: ^(https://[^[:space:]]+|urn:[A-Za-z0-9][A-Za-z0-9:._-]+)$
                type: string
              inputs:
                description: Inputs compose named output ports from other data products.
                items:
                  description: InputPort declares a composed input from another data
                    product.
                  properties:
                    name:
                      description: Name is unique within the consuming product.
                      maxLength: 63
                      pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                      type: string
                    productRef:
                      description: ProductRef selects the producing data product and
                        output port.
                      properties:
                        name:
                          description: Name is the referenced DataProduct name.
                          maxLength: 63
                          pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                          type: string
                        namespace:
                          description: Namespace defaults to the consuming product's
                            namespace.
                          maxLength: 63
                          pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                          type: string
                        output:
                          description: Output names the referenced product's output
                            port.
                          maxLength: 63
                          pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                          type: string
                      required:
                      - name
                      - output
                      type: object
                  required:
                  - name
                  - productRef
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
              name:
                description: Name is the human-readable product name.
                minLength: 1
                type: string
              outputs:
                description: Outputs are the product's published data interfaces.
                items:
                  description: OutputPort publishes one stable, machine-readable data
                    interface.
                  properties:
                    contractUrl:
                      description: |-
                        ContractURL is an HTTPS machine-readable OpenAPI, AsyncAPI, GraphQL schema,
                        DCAT document, or Arrow Flight service description.
                      pattern: ^https://[^[:space:]]+$
                      type: string
                    mediaType:
                      description: MediaType documents the primary response or message
                        representation.
                      type: string
                    name:
                      description: Name is unique within the product and is used by
                        composed inputs.
                      maxLength: 63
                      pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                      type: string
                    protocol:
                      description: Protocol selects the contract family used by this
                        interface.
                      enum:
                      - OpenAPI
                      - AsyncAPI
                      - GraphQL
                      - DCAT
                      - ArrowFlight
                      type: string
                    url:
                      description: URL is the HTTPS runtime endpoint consumers use.
                      pattern: ^https://[^[:space:]]+$
                      type: string
                  required:
                  - contractUrl
                  - name
                  - protocol
                  - url
                  type: object
                minItems: 1
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
              owner:
                description: Owner is accountable for availability, quality, and access
                  decisions.
                properties:
                  name:
                    description: Name is the owner or team display name.
                    minLength: 1
                    type: string
                  url:
                    description: URL points to current ownership or support information.
                    pattern: ^https://[^[:space:]]+$
                    type: string
                required:
                - name
                type: object
              ui:
                description: UI is an optional independently deployed product interaction
                  surface.
                properties:
                  title:
                    description: Title is the accessible label hosts present for the
                      embedded surface.
                    minLength: 1
                    type: string
                  url:
                    description: URL is loaded by compatible hosts in a restricted
                      sandbox.
                    pattern: ^https://[^[:space:]]+$
                    type: string
                required:
                - title
                - url
                type: object
              version:
                description: Version is the product contract's semantic version.
                pattern: ^v[0-9]+\.[0-9]+\.[0-9]+(?:-[0-9A-Za-z.-]+)?$
                type: string
            required:
            - description
            - id
            - name
            - outputs
            - owner
            - version
            type: object
          status:
            description: DataProductStatus reports observed composition and readiness.
            properties:
              conditions:
                description: Conditions report readiness and actionable dependency
                  failures.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: ObservedGeneration is the generation reflected in Conditions.
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
