---
name: "TODOs"
permissions: {}
on:
  workflow_dispatch:
  push:
    branches: [main]

jobs:
  todos:
    permissions:
      contents: read # Scan the repository source.
      issues: write # Create and update TODO tracking issues.
    uses: devantler-tech/actions/.github/workflows/scan-for-todo-comments.yaml@6f47d8ca360fa3058f57175d54fa148deac08983 # v13.6.6
    secrets:
      APP_PRIVATE_KEY: ${{ secrets.APP_PRIVATE_KEY }}
