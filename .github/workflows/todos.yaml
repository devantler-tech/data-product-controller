---
name: "TODOs"
permissions: {}
on:
  workflow_dispatch:
  push:
    branches: [main]

jobs:
  todos:
    permissions:
      contents: read # Scan the repository source.
      issues: write # Create and update to-do tracking issues (lowercase: the scanner files the uppercase marker).
    uses: devantler-tech/actions/.github/workflows/scan-for-todo-comments.yaml@ee4948f9372d00126a24ce30db288294b86568d1 # v13.10.0
    secrets:
      APP_PRIVATE_KEY: ${{ secrets.APP_PRIVATE_KEY }}
