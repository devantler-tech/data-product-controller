---
name: "TODOs"
permissions: {}
on:
  workflow_dispatch:
  push:
    branches: [main]

jobs:
  todos:
    permissions:
      contents: read # Scan the repository source.
      issues: write # Create and update to-do tracking issues (lowercase: the scanner files the uppercase marker).
    uses: devantler-tech/actions/.github/workflows/scan-for-todo-comments.yaml@7a19c1d0f32e5a99a38d0a4106f1b2cbd76c99e4 # v13.6.7
    secrets:
      APP_PRIVATE_KEY: ${{ secrets.APP_PRIVATE_KEY }}
