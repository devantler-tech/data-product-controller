---
name: "TODOs"
permissions: {}
on:
  workflow_dispatch:
  push:
    branches: [main]

jobs:
  todos:
    permissions:
      contents: read # Scan the repository source.
      issues: write # Create and update TODO tracking issues.
    uses: devantler-tech/actions/.github/workflows/scan-for-todo-comments.yaml@759ff9b2526cd1b142b49c5dc1616118a1ef44d9 # v13.6.0
    secrets:
      APP_PRIVATE_KEY: ${{ secrets.APP_PRIVATE_KEY }}
