---
name: "TODOs"
permissions: {}
on:
  workflow_dispatch:
  push:
    branches: [main]

jobs:
  todos:
    permissions:
      contents: read # Scan the repository source.
      issues: write # Create and update TODO tracking issues.
    uses: devantler-tech/actions/.github/workflows/scan-for-todo-comments.yaml@3c3a6348b3e7aacc995ae49fc60032f95f70a779 # v13.6.5
    secrets:
      APP_PRIVATE_KEY: ${{ secrets.APP_PRIVATE_KEY }}
